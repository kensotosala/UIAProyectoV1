@model UI.Models.Diagnostico

@{
    ViewBag.Title = "eliminarDiagnostico";
}

<h2>Eliminar Diagnostico</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Diagnostico</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TN_IdCita)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TN_IdCita)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TC_DscDiagnostico)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TC_DscDiagnostico)
        </dd>
    </dl>

    @using (Html.BeginForm("accionesEjecucion", "Diagnostico", FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        @Html.HiddenFor(model => model.TN_IdDiagnostico)


        <div class="form-actions no-color mt-2">
            <button type="submit" name="accionEjecutar" value="Borrar" class="btn btn-warning" formaction="@Url.Action("accionesEjecucion", "Diagnostico")">
                Eliminar 🗑️
            </button> |
            @Html.ActionLink("Regresar", "obtenerDiagnosticos")
        </div>
    }
</div>