@model UI.Models.Mascotas

@{ Layout = "~/Views/Shared/_LayoutObtener.cshtml"; }

<main>
    <section class="container px-lg-5">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card mt-3 mb-3 cardWrapper">
                    <div class="card-body">
                        <div class="row">
                            <div class="col text-center">
                                <img src="/assets/user.png" class="img-fluid rounded-top customImg" alt="" />
                            </div>
                        </div>
                        <div class="row">
                            <div class="col mb-2 text-center">
                                <h3 class="cardTitle">
                                    ¿Estás seguro que deseas eliminar?
                                </h3>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col">
                                @using (Html.BeginForm("accionesEjecucion", "Mascotas", FormMethod.Post))
                                {
                                    @Html.AntiForgeryToken()
                                    @Html.HiddenFor(model => model.TN_IdMascota)

                                    <dl>
                                        <dt class="form-label lblCustom">Nombre:</dt>
                                        <dd>@Model.TC_NombreMascota</dd>

                                        <dt class="form-label lblCustom">ID Cliente</dt>
                                        <dd>@Model.TN_IdCliente</dd>
                                    </dl>

                                    <div class="mb-3">
                                        <div class="text-center">
                                            <button type="submit"
                                                    name="accionEjecutar"
                                                    value="Borrar"
                                                    class="btn btn-primary w-75 btnEliminar"
                                                    formaction="@Url.Action("accionesEjecucion", "Mascotas")">
                                                Eliminar
                                            </button>
                                        </div>
                                    </div>
                                }
                                <div class="mb-3">
                                    <div class="text-center">
                                        <a href="@Url.Action("obtenerMascotasENT", "Mascotas")"
                                           class="btn btn-primary w-50 btnAgregar">Regresar</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>